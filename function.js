function studentForm (studentID, studentName, studentGrade){
    var id = studentID;
    var name = studentName;
    var grade = studentGrade;
    console.log(`You are ${name} with id ${id} at grade ${grade}`)
}

studentForm(144,"Nova",11)